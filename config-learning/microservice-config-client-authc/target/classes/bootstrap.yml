spring:
  cloud:
    config:
      #uri: http://user:password123@localhost:8080 # curl style
      uri: http://localhost:8080 
      username: user
      password: password123
      profile: dev
      #profile: production
      label: master   # 当configserver的后端存储是Git时，默认就是master 
  application:
    name: foobar
    
# 访问路径：   
# http://localhost:8081/profile
    